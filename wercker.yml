box: nodesource/trusty
build:
  steps:
    - npm-install

    - wacul/store-link:
        type: restore
        prefix: node_modules/.bin

    - script:
        name: build
        code: npm run build
